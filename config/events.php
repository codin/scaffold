<?php 

/**
 * Define events & listeners in here
 */

return [
    
    /**
     * Define your events here and attach
     * a pretty name to them so that you can
     * fire them throughout your application.
     *
     * Note: Conventionally namespace the 
     * name of the event with '.'
     */
    'events' => [
        // 'my.event' => \App\Events\MyEvent::class
    ],

    /**
     * Add your listeners to this array so that 
     * we can bootstrap them when we start the 
     * application.
     */
    'listeners' => [
        // \App\Listeners\MyEventListener::class
    ],
];